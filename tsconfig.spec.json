/* To learn more about this file see: https://angular.io/config/tsconfig. */
// This is a special tsconfig.json file that is used only when running ng test.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "module": "commonjs",
    "types": ["jest"]
  },
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts"]
}
